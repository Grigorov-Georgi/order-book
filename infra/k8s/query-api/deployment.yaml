apiVersion: apps/v1
kind: Deployment
metadata:
  name: query-api
  namespace: order-book
spec:
  replicas: 2
  selector:
    matchLabels:
      app: query-api
  template:
    metadata:
      labels:
        app: query-api
    spec:
      containers:
        - name: query-api
          image: order-book/query-api:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: query-api
  namespace: order-book
spec:
  selector:
    app: query-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
